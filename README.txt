ZenJMX is built using Apache Maven.  Download and install Maven and
then get to work using "mvn" commands.

Here are some quick hints about some useful maven phases to invoke:
  * mvn assembly:assembly
    - Creates a binary distribution of ZenJMX

  * mvn exec:exec:
    - Runs the Main ZenJMX class
